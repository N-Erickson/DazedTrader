# Go build artifacts
dazedtrader
debug
debug_auth
test_auth
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go test binary, built with `go test -c`
*.test

# Go coverage files
*.out

# Go dependency modules
go.sum

# Authentication & API credentials (CRITICAL - NEVER COMMIT!)
.token
*.token
auth_token.json
api_key.txt
*.key
*.pem
*.p12
*.pfx
credentials.json
secrets.json
auth_data.json
robinhood_auth.json

# API keys and private keys
*apikey*
*private_key*
*api_secret*
*access_token*
*refresh_token*

# Local storage files (may contain sensitive data)
local_storage.json
user_data.json
session.json
*.session

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs (may contain sensitive info)
*.log
debug.log
error.log
api.log

# Config files with credentials
config.json
.env
.env.local
.env.production
.env.staging

# Backup files that might contain credentials
*.bak
*.backup
*.old
*.orig

# Test files with real credentials
test_auth_real.go
*_with_real_keys*